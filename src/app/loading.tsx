import { TbLoader3 } from "react-icons/tb";
const loading = () => {
    return (
        <div className="flex flex-col items-center justify-center h-screen">
            <TbLoader3 className="text-5xl animate-spin text-primary" /></div>
    );
};

export default loading;